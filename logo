import React, { Component } from 'react';
import {

StyleSheet,
Text,
View,
TextInput,
TouchableOpacity,

} from 'react-native';

export default class logo extends Component <()> {
render(){
return(
<View style = {style.container}>
<Image style = {{ width:40, height:70}}source = {requiro('../images/logo.png')}/>
<Text style = {style.logoText}>Welcome to My Apps.</Text>
</View
)
}
}

const style = StyleSheet.create({
container : { flewGrow:1, justifyContent:'flex-end', alignItem:'center'}
logoText : {marginVertical:15, fontSizew
